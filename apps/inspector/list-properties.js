// List of static properties used in ispector table coIumns.

'use strict';

var Set = require('es6-set');

module.exports = new Set([
	'isApproved', 'isRejected', 'isSubmitted', 'certificates/dataSnapshot/jsonString',
	'submitterType'
]);
